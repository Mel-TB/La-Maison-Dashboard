import { StyledHeader } from "./Header.styles";

const Header = () => {
  return (
    <StyledHeader
      role='row'
      // $columns={columns}
    >
      HEADER
    </StyledHeader>
  );
};

export default Header;
